export const object = `todoList : id, value, status 값을 가진 객체 리스트\n
postTodo : id, value, status 값을 가진 객체를 parameter로 전달 받아 todoList에 추가하는 작업\n
putTodo : id, value, status 값을 가진 객체를 parameter로 전달 받아 id가 같은 객체의 속성을 수정하는 작업\n
deleteTodo : id를 parameter로 전달 받아 id가 같은 객체를 todoList에서 삭제하는 작업\n
getCnt : status를 parameter로 전달 받아 동일한 상태 값을 가진 객체의 값을 반환하는 작업
`;

export const app = `TodoObject 클래스의 instance 생성 후 method를 이용하여 Todo List 로직 사용 가능`;

export const html = ``;

export const appCss = ``;

export const inputCss = ``;

export const todoCss = ``;
